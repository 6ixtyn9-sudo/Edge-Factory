# Edge Factory — Executive Forensic Brief

**Codename:** ANTIGRAVITY
**Subject:** Edge Factory Consensus Pipeline
**Status Date:** July 5, 2026

## TL;DR Pipeline Health
The Edge Factory pipeline is a highly robust software engineering achievement suffering from severe quantitative alpha deterioration. The architecture (DuckDB materialized views, automated CI GitHub Actions) runs flawlessly and scales beautifully. However, the true predictive edge has decayed to near-zero, and the system is bleeding edge through structural errors in execution (CLV) and classification (Veto Inversion). The underlying matching logic relies on a brittle 9-character truncation that secretly cross-pollinates disparate teams, polluting the core validity of the metrics.

## Veto Logic: Severe Inversion
The Purity and Veto framework is operating exactly backwards. The pipeline is actively suppressing the most profitable picks (`SKIPPED_VETO` ROI = +15.05%) while explicitly passing marginal/negative expectation picks (`CAUTION` ROI = -8.44%). 
- **Verdict:** The veto logic is predicting the wrong way. A "Veto Flip" (trading what the system says not to trade) is highly advised until the Bayesian context smoothing is re-tuned.

## CLV Health: Complete Execution Failure
The pipeline is executing blindly into aggressively sharpening markets. 
- 0 out of 75 measured picks beat the closing line probability (p-value ≈ 2.6e-23). 
- The average raw odds drift is +0.04 against us, meaning we are consistently taking the worst possible price right before the bookmakers widen the spread or correct their lines against the position. 
- **Verdict:** We are systemic losers to the closing line. The system is operating too far in advance of the match or using extremely stale scraping, losing all executable edge to market efficiency.

## Architecture Health vs Decay Velocity
The DuckDB architecture easily handles the scale and throughput required to mine consensus edges across ~43,000 matches. However, the quantitative alpha half-life is significantly shorter than the validation window. Edges decay and go dead before they can accumulate enough validation `n` to safely execute at size. Furthermore, the `run_soft` wrapper in `daily.py` hides vital dispatch errors (like the WhatsApp `404` endpoint bug), creating a false sense of operational security. The `norm_team` 9-character collision is actively destroying the validity of the underlying data warehouse joins.

## FINAL VERDICT: PAUSE & RE-TUNE
**Defense of Verdict:**
To deploy capital into this pipeline *today* is to willingly execute negative-expectation wagers against perfectly efficient short-odds bookmakers. The 0% CLV beat rate proves that whatever edge the consensus miner finds, it is destroyed by execution delay. Furthermore, the `norm_team` bug means you cannot mathematically trust the `valid_roi` metrics. 

You must PAUSE live flat betting immediately. 

The system requires exactly 14 days of downtime to execute the Tactical Roadmap: fixing the 9-char joining bug, flipping the VETO logic into shadow mode, implementing the CLV IP-delta gate, and changing from 1u flat staking to Kelly/4. Only after these structural leaks are plugged can the pipeline be unpaused into FULL DEPLOY.
