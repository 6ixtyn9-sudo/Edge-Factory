# Actionable 14-Day Tactical Roadmap

| Day | Owner | Action | Value | Risk of Inaction |
|---|---|---|---|---|
| Day 1 | MAINTAINER | **Fix `norm_team` Collision:** Increase truncation length from 9 to 15 chars in `util.py`. Wipe `warehouse.duckdb` and run `scripts/build_warehouse.py --full`. | Prevents catastrophic data corruption where distinct teams (e.g. `launcesto`) are merged, polluting all train/valid logic. | **CRITICAL:** Total loss of predictive validity; pipeline continues generating phantom edges. |
| Day 2 | MAINTAINER | **Fix `whatsapp.py` & Silent Fails:** Correct `whatsapp.php` endpoint. Remove `run_soft` wrapper in `daily.py` for dispatch scripts. | Ensures immediate CI failure when messaging breaks, stopping silent drops. | **HIGH:** System operates silently while zero signals reach the user. |
| Day 3 | SYSTEM | **Deploy Veto-Flip (Shadow Mode):** Merge `purity_registry_inverted_proposal.json` into production. Add `VETO_FLIP` bucket to track the +15% ROI cohort. | Captures the inverted alpha discovered during the Veto autopsy without risking immediate flat-bet capital. | Opportunity cost of ~15% ROI discarded daily. |
| Day 4 | MAINTAINER | **Pin Dependencies:** Run `pip freeze > requirements.txt` to lock exact versions of `supabase`, `pandas`, `duckdb`, etc. | Secures the pipeline against upstream breaking changes (e.g. pandas 3.0). | Builds will eventually fail organically, destroying the daily cron. |
| Day 5 | SYSTEM | **Odds Source Filtering:** Demote `zulubet` and hardcode priority to `forebet_best` in `picks_today.py` logic. | Instantly improves execution ROI by ~0.50% by avoiding toxic odds sources. | Continued bleeding from systematic −17% ROI source. |
| Day 7 | SYSTEM | **CLV Protection Gate (Dry Run):** Implement `ip_delta >= -0.25%` gate logic in `picks_today.py` using live odds API right before push. | Cuts out systematic adverse execution paths before capital is deployed. | −8.44% ROI bleed continues in the CAUTION bucket. |
| Day 10 | MAINTAINER | **Staking Migration (Kelly/4):** Transition from flat 1u staking to dynamic volatility-managed Kelly/4 sizing algorithm. | Controls maximum drawdown risk (historically 12% ruin probability at flat 1u). | High risk of bankroll zeroing during prolonged negative variance runs. |
| Day 12 | SYSTEM | **Promote 3way min_p≥60:** Formalize the best-performing `3way-unanimous min_p>=60 avg_p>=60` rule to highest dispatch priority. | Amplifies volume on the only truly statistically significant (+32.6% ROI) validated edge. | Capital is allocated to lower-tier edges instead of the prime alpha. |
| Day 14 | MAINTAINER | **Power Threshold Re-Evaluation:** Stop/Continue review. Re-evaluate live `CAUTION` and `VETO_FLIP` buckets at n=100. | Determines if edges survive the mathematical confidence interval narrowing. | Blind betting into structural decay. |
